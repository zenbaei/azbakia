const ANDROID_LOCALHOST = '10.0.2.2';
const REOMTE_SERVER = 'zenbaei.ddns.net';
const SERVER_PORT = '3500';
const SERVER = REOMTE_SERVER;
export const APP_NAME = 'azbakia';
export const APP_REST_API = `https://${SERVER}:${SERVER_PORT}/api/${APP_NAME}`;
export const EMAIL_REST_API = `https://${SERVER}:${SERVER_PORT}/api/email`;
export const STATIC_FILES_URL = `https://${SERVER}:${SERVER_PORT}/static`;
